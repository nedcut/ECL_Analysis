# Compatibility shim — dependencies are now declared in pyproject.toml.
# See README.md "Development" for the recommended `pip install -e '.[audio,interactive-plots,dev]'` flow.
-e .[audio,interactive-plots,dev]
