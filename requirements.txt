# Compatibility shim — dependencies are now declared in pyproject.toml.
# This installs the package with all optional extras (audio + interactive plots).
# See README.md "First-Time Setup" for the recommended `pip install '.[audio,interactive-plots]'` flow.
-e .[audio,interactive-plots]
